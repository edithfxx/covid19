module.exports = {
  /**
   * 使用相對路徑，讓打包結果可部署於任意子目錄。
   *
   * 預設值 "/" 會讓資源指向網域根目錄，部署到
   * GitHub Pages 的 /covid19/ 之下會全部 404。
   *
   * 本專案為 hash 路由，文件位置恆為站台根目錄，
   * 因此相對路徑在各種部署位置都能正確解析。
   */
  publicPath: "./",
};
